import { describe, it } from "node:test";

describe("Memory", () => {
  it.todo("Should store the 3-digit decimals of VX at the address I on 0xFX33");

  it.todo("Should store V0 to VX at I and set I=I+X+1 on 0xFX55");

  it.todo("Should load V0 through VX at I on set I=I+X+1 on 0xFX65");
});
