import { describe, it } from "node:test";

describe("Inputs", () => {
  it.todo("Should skip if keydown = VX on 0xEX9E");

  it.todo("Should skip if keydown != VX on 0xEXA1");

  it.todo("Should wait for next key press and save code to VX on 0xFX0A");

  it.todo("should initialize timer to VX on 0xFX15");
});
